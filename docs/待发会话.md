规划表解析结果部分单元格内容未完整解析，显示如图；
规划表有两层逻辑，第一部分，活动信息；第二部分，每个活动对应的商品信息；如图中全选，E2-E9是活动基本信息，D2-D9是活动信息对应的字段名，E11开始到这列最后一行是
除了起始位置，还需要用户选择活动参数：活动类型、活动机制、机制力度、活动时间、活动区域/渠道、优惠券类型，预算分配等对应的
删除执行顺序相关逻辑，按我下面描述重新编写活动汇总表与活动对应UPC表生成逻辑：
1、从起始位置起，每列为一个独立活动，如果当列对应的行，起始列左侧标题为活动机制，则根据满xx拆分成两个活动（由于用户描述问题，此处可引入AI来辅助进行，优先选用Deepseek）；
2、