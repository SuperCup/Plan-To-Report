# 用户足迹

## 2026-05-20

### 提问：运行项目看下效果

- 在 `dist/PlanToReport/` 目录启动 `PlanToReport.exe`
- 程序成功运行，窗口标题为「规划转提报工具 - 批量Sheet版」

### 提问：总结一下当前的流程与逻辑

- 梳理了 src 下全部源码模块
- 输出文档：[当前流程与逻辑总结.md](./当前流程与逻辑总结.md)

### 提问：上传 Excel 解析后需支持预览内容，以便选取规划表起始执行位置

- 新增 `src/plan_to_report/ui/excel_preview.py`：原始网格预览，点击选取起始/结束单元格
- 改造 `main_window.py`：每个 Sheet 独立执行区域；指定「规划表」时自动打开预览
- 需重新 `build.bat` 打包后 dist 版本才生效

## 2026-05-21

### 提问：启动项目

- 使用 `dev_start.bat` 从源码启动（`pythonw -m plan_to_report`）
- 窗口标题：「规划转提报工具 - 批量Sheet版」

### 提问：调整活动汇总表输出字段与 UPC 关联

- 按图片字段重排 `活动汇总表`，明确来源字段按规则映射，无来源字段输出空值
- `活动`、`机制名称` 取 `活动机制`；`渠道` 取 `活动区域/渠道`；`机制范围` 取 `活动类型`
- `券名称` 按 `康师傅 + 优惠券类型` 拼接；`优惠券类型` 默认 `同享`；`机制类型` 默认 `商品券`
- 在 `活动汇总表` 与 `活动对应UPC表` 增加 `自定义编码`，用于精确对应活动与 UPC 明细

### 提问：重新启动看下效果

- 结束旧进程后执行 `dev_start.bat` 从源码重启

### 提问：规划表解析结果部分单元格内容未完整解析，单元格内容展示不全

- 纵向合并单元格改为合并区域内全部填充原值，避免仅末行有内容
- 预览表格启用自动换行、完整 tooltip，并按内容自动调整列宽/行高

### 提问：纵向合并拆分后原内容只填充至最后一个单元格

- 恢复 `excel_io` 纵向合并规则：拆分后仅末行单元格保留原值，其余置空

### 提问：启动看下效果

- 执行 `dev_start.bat` 从源码重启应用

### 提问：规划表双层逻辑与 UPC 生成设计

- 输出设计文档：[规划表双层逻辑与UPC生成设计.md](./规划表双层逻辑与UPC生成设计.md)
- 待确认：活动列边界、品牌键、勾选符号、匹配表结构、机制拆分规则等 10 项

### 提问：确认规划表双层逻辑 10 项规则

- 已更新设计文档 v0.2：[规划表双层逻辑与UPC生成设计.md](./规划表双层逻辑与UPC生成设计.md)
- 要点：用户勾选活动列；仅 C 子品牌；单元格可写口味/合写拆分；匹配逻辑→商品清单全量 UPC；机制拆分共用勾选；备注按字段拼接；四步向导替代执行顺序

### 提问：开始 P0 / 继续

- 新增 `plan_config.py`、`plan_parser.py`、`plan_outputs.py`、`ui/plan_wizard.py`
- 规划表四步向导：框选区域 → 字段映射 → 勾选活动列 → 商品区参数
- `main_window` 指定规划表时走专用管线，生成活动汇总表；UPC 表 P0 占位
- 机制 `①②` 拆分、备注 `+` 拼接、活动列勾选

### 提问：下一步（P1）

- 新增 `plan_participation.py`、`product_matcher.py`、`sheet_inputs.py`
- 矩阵勾选（√ / 单元格口味 / 合写拆分）→ 商品匹配逻辑 → 商品清单，多 UPC 全部输出
- 活动机制拆分后共用同一列勾选，每条活动均展开 UPC 行

### 提问：继续 P2

- 新增 `templates/口味别名.json`、`flavor_aliases.py`
- 合写口味按品牌+规格块匹配 C 列具体口味行；√ 在合并规格行时展开块内所有口味行
- D/C 列纵向合并支持 forward fill（规格、子品牌）
- 选品逻辑括号「不要」规则过滤商品清单（如整箱、渠道自组装）

### 提问：继续 P3+P4，实现所有模块后统一联调

- P3：`deepseek_assist.py`，向导可启用 AI；机制拆分/歧义口味回退
- P4：多行包装单元格（口味行+规格行）；`plan_pipeline.py` 统一入口
- 联调：`scripts/run_plan_integration.py`、`tests/test_integration_plan.py`
- 文档：[P3-P4联调说明.md](./P3-P4联调说明.md)

### 提问：DeepSeek 的 Key 改成可配置的

- 新增 `config/app_settings.json` 持久化 DeepSeek 配置（Key / 模型 / 接口 / 开关）
- 向导步骤 ④ 可编辑并「保存 DeepSeek 配置」；执行转换时自动保存
- 示例文件：`config/app_settings.example.json`

### 提问：将所有修改打包，并在 GitHub 创建仓库提交项目

- 已执行 `build.bat`，产物：`dist_release\PlanToReport\PlanToReport.exe`
- 已执行 `package_release.ps1`，离线包：`releases\PlanToReport-win64-*.zip`
- 已 `git init` 并完成本地首次提交（源码，不含 dist_release）
- 已推送到 https://github.com/SuperCup/Plan-To-Report （分支 `main`）
